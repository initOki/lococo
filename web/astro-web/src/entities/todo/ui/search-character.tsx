import './style.scss';

const SearchCharacter = () => {
  return (
    <div className="flex">
      <input type="text" className="default-input" />
      <button className="default-button">검색</button>
    </div>
  );
};

export default SearchCharacter;
